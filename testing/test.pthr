
func deref_num = (num_to_get: Int^) -> Int {
	return num_to_get.^;
}


func set_ptr_to_num = (ptr: Int write, num: Int) -> Void {
	ptr = deref_num(addr num);
}



func main = () #entry -> Int {
	var foo: Int = 16;
	var bar: Int = 92;

	set_ptr_to_num((addr foo).^, (addr bar).^);


	return foo;
}
